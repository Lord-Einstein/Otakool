{% extends 'base.html.twig' %}

{% block title %}{{ 'app.name'|trans }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('styles/login.css') }}">
{% endblock %}

{% block body %}
    <div class="login-container">

        <!-- Left Side - Image (Desktop only) -->
        <div class="login-image-section">
            <img src="{{ asset('img/login_img.png') }}"
                 class="login-bg-image"
                 alt="Manga Universe">
            <div class="image-overlay-gradient"></div>
        </div>

        <!-- Right Side - Form -->
        <div class="login-form-section">
            <div class="form-wrapper">

                <!-- Header -->
                <div class="login-header">
                    <h1 class="brand-title">{{ 'login.title'|trans }}</h1>
                    <div class="subtitle-container">
                        <p id="dynamic-subtitle" class="dynamic-subtitle">INITIALISATION DU SYSTÈME...</p>
                    </div>
                </div>

                <div class="laser-divider"></div>

                <!-- Error Alert -->
                {% if error %}
                    <div class="error-alert">
                        <i class="fa-solid fa-triangle-exclamation pulse-icon"></i>
                        <span>{{ error.messageKey|trans(error.messageData, 'security') }}</span>
                    </div>
                {% endif %}

                <!-- Login Form -->
                <form method="post" class="login-form">

                    <!-- Email Input -->
                    <div class="input-group">
                        <label class="input-label">{{ 'login.email'|trans }}</label>
                        <div class="input-wrapper glass-input">
                            <div class="input-icon">
                                <i class="fa-solid fa-at icon-animate"></i>
                            </div>
                            <input type="email"
                                   value="{{ last_username }}"
                                   name="_username"
                                   class="form-input"
                                   placeholder="email@otakool.com"
                                   autocomplete="email"
                                   required
                                   autofocus>
                        </div>
                    </div>

                    <!-- Password Input -->
                    <div class="input-group">
                        <label class="input-label">{{ 'login.password'|trans }}</label>
                        <div class="input-wrapper glass-input">
                            <div class="input-icon">
                                <i class="fa-solid fa-fingerprint icon-animate"></i>
                            </div>
                            <input type="password"
                                   name="_password"
                                   id="password"
                                   class="form-input"
                                   placeholder="∷ ∷ ∷ ∷ ∷ ∷ ∷ ∷"
                                   autocomplete="current-password"
                                   required>
                            <button type="button" id="togglePassword" class="toggle-password">
                                <i class="fa-solid fa-eye mythic-eye" id="eyeIcon"></i>
                            </button>
                        </div>
                    </div>

                    <!-- Remember Me & Forgot Password -->
                    <div class="form-options">
                        <label class="custom-checkbox">
                            <input type="checkbox" name="_remember_me" id="remember_me">
                            <span class="checkbox-custom">
                                <i class="fa-solid fa-hands-praying checkbox-icon"></i>
                            </span>
                            <span class="checkbox-label">{{ 'login.remember_me'|trans }}</span>
                        </label>
                        <a href="#" class="magic-link">{{ 'login.forgot_pass'|trans }}</a>
                    </div>

                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                    <!-- Submit Button with Portal -->
                    <button type="submit" class="submit-button">
                        <div class="portal-icon">
                            <img src="{{ asset('img/portal.png') }}" alt="Portal" class="portal-image">
                        </div>
                        <div class="button-text-container">
                            <div class="letter-block"><span>T</span></div>
                            <div class="letter-block"><span>É</span></div>
                            <div class="letter-block"><span>L</span></div>
                            <div class="letter-block"><span>É</span></div>
                            <div class="letter-block"><span>P</span></div>
                            <div class="letter-block"><span>O</span></div>
                            <div class="letter-block"><span>R</span></div>
                            <div class="letter-block"><span>T</span></div>
                            <div class="letter-block"><span>A</span></div>
                            <div class="letter-block"><span>T</span></div>
                            <div class="letter-block"><span>I</span></div>
                            <div class="letter-block"><span>O</span></div>
                            <div class="letter-block"><span>N</span></div>
                        </div>
                    </button>
                </form>

                <!-- Register Link -->
                <div class="register-section">
                    <a href="#" class="register-text magic-link">{{ 'login.new_here'|trans }}</a>
                </div>

            </div>
        </div>
    </div>

    {% block javascripts %}
        {{ parent() }}
            <script src="{{ asset('scripts/login.js') }}"></script>
    {% endblock %}
{% endblock %}
